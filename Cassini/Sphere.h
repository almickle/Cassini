#pragma once
#include "Entity.h"

class Sphere
	//public Entity
{
public:
	/*Sphere(Graphics& gfx, ResourceManager& manager, const XMFLOAT3& color) : Entity(gfx, manager, XMLoadFloat3(&color), "Models\\icosphere.txt", "VertexShader.cso", "ColorPS.cso") {
		SetModelColor(color);
		SetPosition({ 5.0f, 5.0f, 5.0f });
	}

	void Update(Graphics& gfx, ResourceManager& manager) override {
		UpdateVSData(gfx, manager, GetTransformation());
		XMFLOAT3 buffer = GetModelColor();
		UpdatePSData(gfx, manager, buffer);
	}*/
};

